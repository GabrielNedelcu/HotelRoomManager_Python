# HotelRoomManager_Python

App for the university - interface for a MySQL Database showcasing basic functionality (insert, select, delete, update, inner join etc.) build using Python + fask (using self-build ORM)
